# FitBois 2.0 Environment Variables
# Copy this file to .env and update the values

# ============ FRONTEND (React) ============
# API URL for frontend to connect to backend
# For local development:
REACT_APP_API_URL=http://localhost:5000/api
# For production (Railway will set this automatically, or use your deployed URL):
# REACT_APP_API_URL=https://your-app.railway.app/api

# ============ BACKEND (Express) ============
# Port for the backend server (Railway sets this automatically)
PORT=5000

# Node environment (set to 'production' in Railway)
NODE_ENV=development

# Frontend URL for CORS (optional, defaults to allow all in production)
# FRONTEND_URL=https://your-app.railway.app
